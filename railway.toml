# Railway Dashboard → Service → Settings:
# - Root Directory: set to this backend folder if repo is a monorepo (critical).
# - Custom Start Command (optional): node src/server.js
#
[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
